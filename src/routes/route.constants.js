import { AuthRouteEnum, AppRouteEnum } from "./route.enum";

/**
 * Centralized Route Path Mapping for CricNovas Mobile Application
 * Combines Authentication and App Feature Route Enums
 */
const ROUTE_PATH = {
  AUTH: AuthRouteEnum,
  APP: AppRouteEnum,
};

export default ROUTE_PATH;
